#include "lib/global.h"

/********************
 * Function Decls
********************/

ret_t synchronome_run(
		const uint width,
		const uint height,
		const char* output_dir
);

void synchronome_stop(void);
